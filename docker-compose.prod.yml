services:
  api:
    build: .
    container_name: jobscope-api
    env_file:
      - .env.production
    ports:
      - "8000:8000"
    restart: unless-stopped
    command: >
      sh -c "python -m pipeline.wait_for_db &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000"